{
  "name": "ghost-pop-crosslister",
  "version": "0.1.0",
  "private": true,
  "description": "Manifest V3 cross-listing extension scaffold for Depop, Vinted, Poshmark, and eBay.",
  "scripts": {
    "lint": "echo \"No lint step configured yet\"",
    "check": "node -e \"const fs=require('fs'); JSON.parse(fs.readFileSync('manifest.json','utf8')); console.log('manifest ok')\""
  }
}
